apiVersion: apps/v1
kind: Deployment
metadata:
  name: main-app-dep
spec:
  replicas: 1
  selector:
    matchLabels:
      app: main-app
  template:
    metadata:
      labels:
        app: main-app
    spec:
      volumes:
        - name: files
          persistentVolumeClaim:
            claimName: main-app-claim
      containers:
      - name: main-app-hash
        image: koxanybak/hash
        volumeMounts:
          - mountPath: /app/files
            name: files
      - name: main-app-stamp
        image: koxanybak/stamp
        volumeMounts:
          - mountPath: /app/files
            name: files
